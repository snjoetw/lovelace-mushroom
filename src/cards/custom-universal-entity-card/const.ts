import {PREFIX_NAME} from "../../const";

export const ENTITY_CARD_NAME = `${PREFIX_NAME}-custom-universal-entity-card`;

export const ON_STATES = ["on", "open", "opening", "playing", "cleaning", "unlocked"];
export const CARD_BACKGROUND_OPACITY = 0.15;
export const ICON_BACKGROUND_OPACITY = 0.25;
